# Task: Initialize Project Repository and Build System

## Description
Initialize the git repository, set up the directory structure, and configure the build system using CMake and vcpkg.

## Requirements
- Git repository initialized with .gitignore
- Project structure created:
    - server/
    - clients/
    - docs/
- Build system configured:
    - CMakeLists.txt for root and subprojects
    - vcpkg configuration for dependency management
- Docker support added

## Completed
- [x] Git init
- [x] Basic directory structure
- [x] Root CMakeLists.txt
- [x] vcpkg.json and manifest mode setup
- [x] Dockerfile and compose setup
